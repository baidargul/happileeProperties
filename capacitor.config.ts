import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.happileeproperty.app',
  appName: 'Happilee Property',
  webDir: 'out'
};

export default config;
